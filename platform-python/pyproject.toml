[project]
name = "platform-python"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "vdiquez", email = "vdiquez@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.23,<0.12.0"]
build-backend = "uv_build"
